# CLAUDE.md

## Tech Stack

- **Framework**: Next.js App Router
- **Language**: TypeScript (strict mode)
- **Database**: Postgres via Drizzle ORM
- **Auth**: Clerk
- **API**: tRPC
- **Styling**: Tailwind CSS + shadcn/ui
- **Deployment**: Vercel

## Dependency Direction

```
Types → Config → Repo → Service → Runtime → UI
```

Enforced by `scripts/check-deps-direction.ts`. Never import from a higher layer into a lower layer.

## File Structure

<!-- FILE_STRUCTURE_START -->
Auto-generated by scripts/sync-claude-md.sh — do not edit this section manually
<!-- FILE_STRUCTURE_END -->

## Conventions

### API Responses

All API responses use a consistent shape:

```ts
{ data, error, metadata }
```

Errors always follow:

```ts
{ code, message, details }
```

### Database

- All tables must have: `id`, `created_at`, `updated_at`, `deleted_at`
- Use soft deletes via `deleted_at` unless there is a strong reason not to

### Code Quality

- No `any`, `@ts-ignore`, or `eslint-disable` without an explanatory comment
- No business logic in route handlers — route handlers orchestrate, services execute
- Named exports only (no default exports)
- `const` arrow functions for all components and utilities
- Early returns over nested conditionals
- Max function length: 30 lines
- Max file length: 200 lines

## Testing

- Run `pnpm test` after every change
- Never remove or weaken existing tests
- New features require integration tests
- Use the real database in tests (test schema/transaction rollback pattern)

## Git

- Commit after each meaningful step
- Use conventional commits: `feat:`, `fix:`, `chore:`, `refactor:`, `test:`, `docs:`
- Never commit `.env` or any file containing secrets

## When In Doubt

- Prefer boring, explicit code over clever abstractions
- If something feels wrong, stop and ask before continuing
- Do not invent APIs or imports — check they exist first
- Leave a `// TODO:` comment rather than silently skipping something
